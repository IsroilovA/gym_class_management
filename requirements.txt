# Convenience pointer — installs everything (runtime + dev/test).
# CI and local development: pip install -r requirements.txt
# Docker image uses:        pip install -r requirements/base.txt
-r requirements/dev.txt
